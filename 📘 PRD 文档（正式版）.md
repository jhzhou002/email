# 📘 PRD 文档（正式版）

子邮箱验证码接收 Web 应用（V1.1）**

------

# 1. 产品背景

在 Cloudflare 环境下部署了域名邮件路由（Email Routing 或 Workers），主域名及其子邮箱能够接收来自各类系统的验证码／通知等邮件。为了让用户更方便地管理、查看属于自己子邮箱的验证码邮件，需要建设一套 Web 应用：

- 用户可以注册一个“子邮箱账号”
- 登录后即可在系统内查看该子邮箱收到的验证码邮件
- 管理员可管理所有用户，并配置域名邮件接入（SMTP/IMAP 等参数）

------

# 2. 产品目标

### 🎯 **主要目标**

- 提供一个安全的邮件接收平台，展示子邮箱收到的验证码邮件
- 用户可管理自己的子邮箱邮件（只读）
- 管理员可管理用户与邮件系统配置

### 🎯 **非目标**

- 不提供邮件发送功能
- 不允许用户创建任意域名邮箱（邮箱来源通过规则生成或自行填写，注册时需要与数据库存在的子邮箱进行验证是否存在，在管理员页面加一个子邮箱生成页面，后续用户注册的子邮箱均来自于此）

邮箱配置：

```
DOMAIN=aihubzone.shop
IMAP_SERVER=imap.qq.com    # IMAP服务器地址
IMAP_PORT=993              # IMAP端口，SSL通常为993
IMAP_USER=318352733@qq.com       # 邮箱地址
IMAP_PASS=xjnxfmgzcogfbjij              # 邮箱授权码或密码
```

------

# 3. 用户角色与权限

| 角色         | 权限说明                                                     |
| ------------ | ------------------------------------------------------------ |
| **普通用户** | 注册、登录、查看属于自己子邮箱的邮件                         |
| **管理员**   | 用户管理、邮件查看、系统配置（SMTP/IMAP）、查看日志等全部权限 |

------

# 4. 系统功能结构（总体）

```
前台用户端
    ├── 注册（邮箱+密码）
    ├── 登录（邮箱+密码+数字验证码）
    ├── 邮件收件箱（列表）
    ├── 邮件详情（正文展示 + 自动提取验证码）
    └── 用户设置（修改密码等）

后台管理端
    ├── 管理员登录
    ├── 用户管理
    ├── 邮件列表（全局）
    ├── 域名与邮箱配置（SMTP/IMAP）
    ├── 系统日志（登录日志、邮件抓取日志）
    └── 仪表盘（统计信息）
```

------

# 5. 功能详细说明（FSD）

## 5.1 前台用户端

------

## **5.1.1 用户注册**

用户输入：

- 子邮箱（如：`test@sub.domain.com`）
- 密码

### 注册流程

1. 前端校验邮箱格式是否合法、是否属于允许的域名
2. 后端检查用户是否已存在
3. 后端将用户密码进行 Hash（bcrypt）
4. 将用户写入数据库
5. 返回注册成功

### UI/交互

- 邮箱输入框
- 密码框（强度提示可选）
- 注册成功后跳转登录页

### 异常情况

- 邮箱格式不合法 → 报错
- 邮箱对应域名不在系统允许范围 → 报错
- 邮箱已注册 → 报错

------

## **5.1.2 用户登录**

输入：

- 子邮箱
- 密码
- 数字验证码（简单算术，如 3+7）

### 登录流程

1. 前端展示算术题
2. 用户输入答案
3. 后端校验验证码
4. 校验邮箱密码
5. 登录成功后返回 JWT（或 session）

### 登录保护机制

- 密码错误 5 次 → 自动锁定 10 分钟
- 登录需要人机验证（数字运算）

------

## **5.1.3 邮件列表**

用户登录后看到属于自己邮箱的所有邮件。

### 展示字段：

- 发件人
- 邮件标题
- 时间
- 邮件类型（系统自动识别：验证码/普通邮件）
- 是否已读

### 操作

- 点击查看详情
- 搜索（按标题、发件人）

------

## **5.1.4 邮件详情**

展示：

- 邮件标题
- 时间
- 发件人
- 邮件 HTML 正文
- 邮件纯文本正文

### 自动提取验证码（关键功能）

- 后端解析时自动从正文中提取连续数字（4-8 位）
- 页面展示“验证码：xxxxxx（点击复制）”

------

## **5.1.5 用户设置（可选）**

- 修改密码
- 查看当前绑定子邮箱
- 退出登录

------

# 5.2 后台管理端

------

## **5.2.1 管理员登录**

- 独立后台登录入口：`/admin/login`
- 需输入账号密码 + 人机验证（可选）
- 登录后进入仪表盘

------

## **5.2.2 仪表盘**

展示系统关键指标：

- 今日邮件数
- 用户总数
- 活跃用户
- 最近错误日志
- 邮件接收成功率

------

## **5.2.3 用户管理**

| 字段     | 描述                   |
| -------- | ---------------------- |
| 用户邮箱 | 账号唯一标识           |
| 注册时间 | 时间                   |
| 状态     | 正常 / 禁用            |
| 邮件数量 | 收件总量               |
| 操作     | 查看详情 / 禁用 / 删除 |

管理员可：

- 搜索用户
- 禁用用户（禁用后无法登录）
- 删除用户（所有邮件一起删除）

------

## **5.2.4 邮件管理（管理员可查看所有邮件）**

列表字段：

- 用户邮箱
- 邮件标题
- 发件人
- 接收时间
- 状态（已解析 / 解析失败）
- 操作（查看邮件详情）

------

## **5.2.5 邮件系统配置（核心）**

### SMTP / IMAP 配置

管理员可输入：

| 配置项            | 示例                                    |
| ----------------- | --------------------------------------- |
| SMTP 地址         | smtp.qq.com                             |
| SMTP 端口         | 587                                     |
| SMTP 用户名       | [example@qq.com](mailto:example@qq.com) |
| SMTP 授权码       | xxxxxx                                  |
| IMAP 地址         | imap.qq.com                             |
| IMAP 端口         | 993                                     |
| 邮箱密码 / 授权码 | xxxxxx                                  |

### 域名相关配置

- 允许注册的邮箱域名（如：`@mail.example.com`）
- 子邮箱前缀规则（是否限制长度、黑名单等）

------

## **5.2.6 系统日志**

记录：

- 用户登录日志
- 管理员操作日志
- 邮件拉取与解析日志
- 错误日志（如解析失败）

------

# 6. 邮件接收与处理流程（MVP）

### ✉️ 邮件流转路径

```
外部系统 → Cloudflare Email Routing → 主邮箱  
→ 后端通过 IMAP 定时拉取 → 邮件解析 →  
根据 "To" 判断属于哪个用户 → 存入数据库
```

------

# 7. 系统流程图（简化）

## 注册流程

```
输入子邮箱 + 密码
    ↓
后端检查邮箱合法性
    ↓
写入数据库
    ↓
注册成功
```

## 登录流程

```
输入账号/密码
    ↓
数字人机验证码校验
    ↓
验证密码
    ↓
返回 token
```

## 邮件处理流程

```
IMAP 拉取 → 解析邮件头与正文 → 提取验证码 →  
匹配用户邮箱 → 写入数据库 → 前端展示
```

------

# 8. 数据库设计（MySQL）

数据库连接信息：

```
host：49.235.74.98
username：remote
pwd：Zhjh0704.
db:email
```



## 用户表 `users`

| 字段               | 类型     | 描述         |
| ------------------ | -------- | ------------ |
| id                 | int      | 主键         |
| email              | varchar  | 子邮箱       |
| password_hash      | varchar  | bcrypt       |
| status             | tinyint  | 0正常 1禁用  |
| failed_login_count | int      | 登录失败次数 |
| created_at         | datetime | 注册时间     |

## 邮件表 `emails`

| 字段 | 类型 | 描述 |
 | id | int | 主键 |
 | user_id | int | 所属用户 |
 | from_addr | varchar | 发件人 |
 | subject | varchar | 标题 |
 | body_html | longtext | HTML |
 | body_text | text | 文本 |
 | code | varchar | 自动提取的验证码 |
 | received_at | datetime | 接收时间 |

## 配置表 `settings`

- smtp_host
- smtp_user
- smtp_pass
- imap_host
- imap_pass
- allowed_domains
- 等等

------

# 9. 技术选型说明（Vue3 + Node.js + MySQL）

## 前端（Vue3）

- Vue 3 + Vite
- UI 框架：Element Plus 或 Naive UI
- 状态管理：Pinia
- 前端路由：Vue Router
- 邮件展示：使用 HTML 渲染 iframe 组件（防 XSS）

## 后端（Node.js）

- 框架：Express / Koa / NestJS（建议 NestJS）
- 邮件拉取：IMAP 库（imapflow）
- 邮件解析：mailparser
- 密码加密：bcrypt
- Token：JWT
- 数据库 ORM：Prisma 或 Sequelize

## 数据库

- MySQL 8.x
- 分表：可后期把邮件表拆分

------

# 10. 安全性需求

### ✔ 密码必须使用 bcrypt 哈希

### ✔ 登录需数字验证码

### ✔ 所有接口需 JWT 校验

### ✔ 邮件 HTML 渲染需 XSS 过滤

### ✔ 限制每 IP 注册频率

### ✔ 管理端需记录所有操作日志

